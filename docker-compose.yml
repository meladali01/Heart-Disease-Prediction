version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - MODEL_PATH=/app/models/knn_final.joblib
      - APP_HOST=0.0.0.0
      - APP_PORT=5000
